# Worker que manda los recordatorios push de la rutina de Verónica.
# No se deployea automáticamente (Nivel 16, deploy manual) — ver README.md
# en la raíz del repo, sección "Recordatorios push", para el setup completo.

name = "rutina-veronica-push"
main = "src/worker.js"
compatibility_date = "2026-08-18"
compatibility_flags = ["nodejs_compat"]

kv_namespaces = [
  { binding = "SUBSCRIPTIONS", id = "5adb4684db5f488cbe13fc3d6ec554cd" }
]

# Horarios de los bloques Lunes-Viernes (ya definidos en app.js), en UTC.
# Argentina es UTC-3 fijo (sin horario de verano desde 2009).
#   Mañana 6:00  ART -> 09:00 UTC
#   Tarde  12:30 ART -> 15:30 UTC
#   Noche  19:00 ART -> 22:00 UTC
#   Aviso de cumpleaños (Nivel 17) 8:00 ART -> 11:00 UTC
# Fin de semana (Nivel 18) — horarios propios, más tarde que los de semana
# porque esos bloques son "Libre" en la rutina:
#   Mañana 9:00  ART -> 12:00 UTC (mismo día, sáb/dom)
#   Tarde  14:00 ART -> 17:00 UTC (mismo día, sáb/dom)
#   Noche  21:00 ART -> 00:00 UTC del día siguiente (cruza medianoche UTC:
#     sábado 21:00 ART = domingo 00:00 UTC, domingo 21:00 ART = lunes
#     00:00 UTC — por eso el cron dispara domingo y lunes en UTC, no
#     sábado/domingo).
# IMPORTANTE (Nivel 18, encontrado al agregar los crons de fin de semana):
# Cloudflare numera el día de semana 1=domingo..7=sábado, DISTINTO del
# estándar 0=domingo..6=sábado — "1-5" no es lunes-viernes acá, es
# domingo-jueves. Se usan siempre abreviaturas de 3 letras (MON, TUE, ...)
# para evitar esta ambigüedad, incluso en los crons de semana que ya
# estaban (se corrigieron de "1-5" a "MON-FRI" en esta misma pasada).
#
# El plan free de Cloudflare permite 5 Cron Triggers POR CUENTA (no por
# Worker) — con 3 de semana + 1 de cumpleaños ya usábamos 4, así que el
# fin de semana entero va en UN solo cron combinado (no uno por bloque):
# dispara a las 00:00, 12:00 y 17:00 UTC en sábado, domingo y lunes (9
# combinaciones), y el código en worker.js decide con la hora/día ART real
# cuál de los 6 disparos válidos es (o no hace nada en los otros 3,
# subproducto inevitable de cruzar horas×días en un solo cron).
[triggers]
crons = [
  "0 9 * * MON-FRI",
  "30 15 * * MON-FRI",
  "0 22 * * MON-FRI",
  "0 11 * * *",
  "0 0,12,17 * * SAT,SUN,MON",
]

# Públicas, no secretas: la public key VAPID viaja igual dentro de app.js.
# VAPID_PRIVATE_KEY se setea aparte con `wrangler secret put`, nunca acá.
[vars]
VAPID_PUBLIC_KEY = "BKS7dha6Jk1ijHBI05bfuxaYk_Q_Lh028EfeUSndMCloJzHrfibh0Nmb18hnKru-dBnwhMG5nl9oFsDQt9NrBu0"
VAPID_SUBJECT = "mailto:gustavopaine@gmail.com"
# Origen permitido para las llamadas de fetch() desde el navegador.
ALLOWED_ORIGIN = "https://gustavopaine.github.io"
